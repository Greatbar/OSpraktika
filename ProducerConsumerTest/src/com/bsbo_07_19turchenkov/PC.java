package com.bsbo_07_19turchenkov;

import java.util.LinkedList;

public class PC {



    /*public synchronized void take() {
        synchronized (list) {
            int value = 1 + (int) (Math.random() * 10);
            for (int i = 0; i < value; i++) {
                list.remove(value);
            }
            System.out.println("Покупатель купил: " + value + " товар");
            System.out.println("Товаров на складе: " + list.size());
            notify();
        }
    }

    public synchronized void put() {
        synchronized (list) {
            int value = 1 + (int) (Math.random() * 100);
            for (int i = 0; i < value; i++) {
                list.add(value);
            }
            System.out.println("Производитель добавил: " + value + " товар");
            System.out.println("Товаров на складе: " + list.size());
            notify();
        }
    }*/
}
